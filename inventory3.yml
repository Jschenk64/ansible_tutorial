---
- hosts: all
  become: yes
  tasks:

    - name: Update repository index
      apt:
        update_cache: yes
      when: ansible_distribution == "Ubuntu"

    - name: Install Nginx
      apt:
        name: nginx
        state: latest
      when: ansible_distribution == "Ubuntu"

